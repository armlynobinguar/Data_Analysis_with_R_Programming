library(readr)

library(readr)
life_expec <- read_csv("life_expec.csv")

life_expec %>% 
  ggplot()

life_expec %>% 
  ggplot(aes(x = Year, y = Avg_Life_Expec))

life_expec %>% 
  ggplot(aes(x = Year, y = Avg_Life_Expec)) +
  geom_line()
